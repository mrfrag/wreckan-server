application: ${project.name}
version: ${project.version}
runtime: java

handlers:
- url: /admin
  jsp: admin.jsp
  login: admin
  secure: always
  name: admin
- url: /report
  servlet: com.github.wreckan.server.ReportServlet
  secure: always
  name: report
   
inbound_services:
- mail

threadsafe: false
